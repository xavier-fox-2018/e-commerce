const jwt = require('jsonwebtoken')
const User = require('../models/user')

const authorization = (req, res, next) => {    
  // console.log('masuk auth==>', req.headers, req.user)
}

  module.exports = authorization